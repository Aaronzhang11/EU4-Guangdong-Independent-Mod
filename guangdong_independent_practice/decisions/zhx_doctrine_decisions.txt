country_decisions = {
    zhx_convene_hundred_schools_debate = {
        major = yes

        potential = {
            zhx_is_lijiao_country = yes
            NOT = { zhx_has_doctrine = yes }
            NOT = { has_country_modifier = zhx_doctrine_change_cooldown }
        }

        allow = {
            is_at_war = no
            stability = 0
            capital_scope = { controlled_by = ROOT }
        }

        effect = {
            country_event = { id = zhx_doctrine.1 }
        }

        ai_will_do = {
            factor = 1
        }
    }

    zhx_review_current_doctrine = {
        major = no

        potential = {
            ai = no
            zhx_has_doctrine = yes
        }

        allow = { always = yes }

        effect = {
            country_event = { id = zhx_doctrine.20 }
        }

        ai_will_do = { factor = 0 }
    }
}
