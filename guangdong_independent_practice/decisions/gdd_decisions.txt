country_decisions = {
    gdd_leave_china = {
        major = yes

        # Reserved interface. Re-enable this decision when the replacement
        # relationship design is implemented.
        potential = { always = no }

        allow = { always = no }

        effect = {
            country_event = { id = gdd_defiance.1 }
        }

        ai_will_do = {
            factor = 0
        }
    }
}
