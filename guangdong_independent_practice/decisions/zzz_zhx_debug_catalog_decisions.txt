# Developer-only catalog entry. The decision remains absent until the human
# player explicitly enables it with the console event zhx_debug.1.
country_decisions = {
    zhx_open_debug_catalog = {
        major = yes

        potential = {
            ai = no
            has_country_flag = zhx_debug_catalog_enabled
        }

        allow = {
            always = yes
        }

        effect = {
            country_event = { id = zhx_debug.10 }
        }

        ai_will_do = { factor = 0 }
    }
}
