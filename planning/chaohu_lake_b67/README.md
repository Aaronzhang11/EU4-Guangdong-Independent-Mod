# B67 巢湖移植

- 参考源：大明日不落 `provinces.bmp`（6400×2560）。
- 来源颜色：`100,14,111`。
- 来源主体包围盒：`5080,890` 至 `5089,894`，共 22 像素、一个四向连通分量。
- 目标位置：当前地图巢湖省（5061）内部，左上角 `4640,867`。
- 目标湖泊 ID：复用已列入 `default.map` 的预留湖泊 4011。

同色来源图中另有一块 10 像素水体，分别接触太平、南京，并非巢湖主体，因此没有移植。几何按 1:1 像素复制，不做平滑或缩放。

`before_patch.png` 与 `after_patch.png` 是仅含实际改动像素的守卫补丁；应用脚本会拒绝覆盖不符合预期的地图状态，并保留现有 BMP 头。
